# ─────────────────────────────────────────────────────────
# Ignore Terraform’s plugin cache and downloaded modules
.terraform/

# Ignore Terraform state files (may contain sensitive data)
*.tfstate
*.tfstate.backup

# Ignore crash logs
crash.log

# Ignore local override files
override.tf
override.tf.json
*_override.tf

# Ignore CLI config (if you ever stash credentials here)
.terraformrc
terraform.rc

# Ignore real variable definitions (keep only terraform.tfvars.example)
terraform.tfvars
*.auto.tfvars

# ─────────────────────────────────────────────────────────
# Keep in repo:
#  - *.tf          (your main.tf, variables.tf, outputs.tf, etc.)
#  - .terraform.lock.hcl
#  - README.md / examples (e.g. terraform.tfvars.example)
terraform/terraform.tfstate.1753424207.backup
projects_to_do.md
